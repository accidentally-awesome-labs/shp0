packages:
  - "apps/*"
  - "packages/*"

# pnpm 11 settings live here (moved from package.json's `pnpm` field).
# Allow these native deps to run their install-time build scripts.
onlyBuiltDependencies:
  - esbuild
  - sharp
